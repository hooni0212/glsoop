# 프로젝트 이력 (아카이브)

이 문서는 완료/보관된 로드맵, PoC, 회고성 문서를 한 곳에 모아 관리하기 위한 아카이브입니다.

---

## 1) 성장 P1 안정화 (완료)

- 원본 브랜치: `feature/growth-p0-uiux-e2e-seed-fix`
- 핵심 결과:
  - `GET /api/growth/dashboard` 도입
  - growth 화면의 dashboard 우선 로드 + legacy fallback 운영
  - claim XP 경로를 `addXp` 유틸 경로로 정리
  - E2E 회귀 테스트(`tests/e2e/growth-dashboard.spec.js`) 추가
- 현재 기준 문서:
  - `docs/참고/API-레퍼런스.md`
  - `docs/서버/API/성장-퀘스트.md`

---

## 2) UI/UX P2 순차 개선안 (보관)

- 원본 브랜치 계획: `feature/uiux-p2-sequential-hardening`
- 범위 요약:
  - 에디터 초안 자동 저장/복구
  - auth form 오류 UX 통일
  - like/bookmark/follow 액션 피드백 공통화
  - 모바일 IA 및 접근성(focus/aria-live) 강화
- 상태: 단계별 실행 로드맵 문서로만 보관 (실행 시점의 코드 기준으로 재계획 필요)

---

## 3) 피드 이미지 렌더링 개념검증(PoC) (2026-02-13, 완료)

- 원본 브랜치: `feat/feed-image-render-poc`
- 핵심 결과:
  - `utils/feedImageRenderer.js` 기반 서버 렌더링 추가
  - `GET /api/feed-images/post/:postId` API 연결
  - Explore 카드에서 렌더 이미지 우선 사용 + 실패 시 fallback
- 후속 운영 고려사항:
  - 캐시 정리 정책(LRU/만료)
  - xlong 콘텐츠 처리 정책

---

## 4) 메인 홈 UX 진단 메모 (보관)

- 주요 제안 축:
  - SEO/OpenGraph 메타 태그 보강
  - 폰트/계절 CSS 로딩 최적화
  - `:focus-visible` 중심 접근성 개선
  - 피드 로딩/빈 상태 시각 피드백 개선
- 상태: 개선 아이디어 아카이브 (구현 여부는 현재 코드 기준으로 재평가)

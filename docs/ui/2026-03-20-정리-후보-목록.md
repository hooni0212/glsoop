# 글숲 정리 후보 목록

- 문서 타입: `Cleanup Candidate List`
- 기준 일자: `2026-03-20`
- 상태: `Draft`
- 범위: `editor3/post3 제외`
- 참고:
  - 프로젝트 전반 레거시 점검 문서: `docs/refactor/2026-03-20-project-legacy-audit.md`

---

## 1. 목적

- 현재 제품 플로우에 직접 쓰이지 않거나
- 데모/실험 성격이 강하거나
- 전역 번들에 섞여 있어 유지 비용이 큰 파일과 구조를 정리 후보로 남긴다.

---

## 2. 우선 정리 후보

### 2.1 UI Kit / Demo 계열 파일군

상태:

- ~~정리 완료~~

후보 파일군:

- `public/css/pages/ui-kit-core-v2.css`
- `public/css/pages/ui-kit-card-showcase.css`
- `public/css/pages/ui-kit-pin.css`
- `public/css/pages/ui-kit-animated-signin-rive.css`
- `public/css/pages/ui-kit-login-forest-lab.css`
- `public/js/uikit-pin-page.js`
- `public/js/ui-kit/*`

메모:

- 현재 제품 주요 HTML 라우트에서 직접 연결되는 흔적이 약하다.
- 내부 시안/실험/디자인 검증용 자산이 섞여 있는 상태로 보인다.
- 즉시 삭제보다는 `legacy-lab` 또는 `archive`로 분리하는 방향이 안전하다.
- 현재 브랜치에서는 제품 플로우와 무관한 demo/UI-kit 자산을 우선 제거했다.

### 2.2 별도 로그인 프리뷰 페이지

상태:

- ~~정리 완료~~

후보 파일:

- `public/html/login-rive-preview.html`

메모:

- 실제 `login.html`이 동일 계열 UI와 스크립트를 사용 중이라 별도 프리뷰 페이지 유지 가치가 낮다.
- 개발용 검증 페이지로 유지할지, 제거할지 결정이 필요하다.
- 현재 브랜치에서는 별도 프리뷰 HTML만 제거하고, 실제 로그인 화면이 사용하는 JS/CSS는 유지했다.

### 2.3 전역 CSS 번들의 실험 페이지 import

상태:

- ~~정리 완료~~

후보 파일:

- `public/css/pages/all.css`

메모:

- 실험/보조 페이지 스타일이 전 페이지 번들에 같이 실리고 있었다.
- 현재 브랜치에서는 아래 정리를 반영했다.
  - `login-rive-preview.css`는 `login.css`로 흡수 후 파일 삭제
  - `editor2.css`, `editor3.css`, `post3.css`는 각 HTML에서 직접 로드하도록 변경

---

## 3. 보류 권장

### 3.1 editor2 계열

관련 파일:

- `public/html/editor2.html`
- `public/js/editor2.js`
- `public/js/editor2LayoutEditor.js`
- `public/css/pages/editor2.css`

메모:

- 실험 성격이 강하지만, 아직 바로 삭제하기엔 문서/링크/검증 맥락이 남아 있다.
- 삭제보다 `legacy-experimental` 성격으로 위치를 낮추는 편이 안전하다.

### 3.2 login-rive-preview.js

관련 파일:

- `public/js/login-rive-preview.js`

메모:

- `login-rive-preview.html`뿐 아니라 실제 `login.html`도 사용 중이다.
- 따라서 프리뷰 페이지와 분리해서 판단해야 하며, 스크립트 자체는 즉시 삭제 대상이 아니다.

---

## 4. 다음 정리 순서 제안

1. UI kit / demo 계열의 실제 참조 경로 재확인
2. 전역 CSS 번들에서 실험 페이지 import 분리
3. `login-rive-preview.html` 유지 여부 결정
4. `editor2` 계열의 역할을 `유지 / 보관 / 제거` 중 하나로 명시
